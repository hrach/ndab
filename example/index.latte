<!DOCTYPE html>
<html>
<head>
	<meta charset=utf-8>
    <title>Ndab demo example</title>
    <link rel="stylesheet" type="text/css" media="screen" href="bootstrap.css">
</head>
<body>

<div class="navbar navbar-fixed-top">
<div class="navbar-inner">
<div class="container">
	<a class="brand">Ndab demo example</a>
</div>
</div>
</div>

<div class="container">
<div class="hero-unit">

	<div class="alert alert-success">
		Celkem nalezeno knih: {count($books)}
	</div>

	<ol>
		{foreach $books as $book}
		<li>
			<strong>{$book->title}</strong> {* using an alias *}

			<ul n:if="count($book->sortedTags)">

				{* enhanced to predefine raleted call *}
				{foreach $book->sortedTags as $tag}
				<li>{$tag->name}</li>
				{/foreach}

			</ul>
		</li>
		{/foreach}
	</ol>

</div>
</div>

</body>
</html>
